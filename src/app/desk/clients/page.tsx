"use client";

// Partner+ desk — the funded book.

import { WorkspaceClients } from "@/components/workspace/workspace-clients";

export default function DeskClientsPage() {
  return <WorkspaceClients basePath="/desk" />;
}
