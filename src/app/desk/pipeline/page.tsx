"use client";

// Partner+ desk — the funding pipeline kanban for deals they own or follow.

import { WorkspacePipeline } from "@/components/workspace/workspace-pipeline";

export default function DeskPipelinePage() {
  return <WorkspacePipeline basePath="/desk" />;
}
