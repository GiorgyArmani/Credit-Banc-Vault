"use client";

// Partner+ desk — every non-funded file they own or follow.

import { WorkspaceProspects } from "@/components/workspace/workspace-prospects";

export default function DeskProspectsPage() {
  return <WorkspaceProspects basePath="/desk" />;
}
