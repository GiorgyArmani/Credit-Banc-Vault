"use client";

// Partner deal desk — every non-funded file they own or follow.

import { WorkspaceProspects } from "@/components/workspace/workspace-prospects";

export default function PartnerProspectsPage() {
  return <WorkspaceProspects basePath="/partner" />;
}
