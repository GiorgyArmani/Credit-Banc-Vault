"use client";

// Partner deal desk — the funding pipeline kanban for deals they own or follow.

import { WorkspacePipeline } from "@/components/workspace/workspace-pipeline";

export default function PartnerPipelinePage() {
  return <WorkspacePipeline basePath="/partner" />;
}
