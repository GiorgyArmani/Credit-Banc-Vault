// Partner deal desk — one-page fast funding form.
//
// Creates the client, sends the magic link, and gets the SignWell application
// signed during the call. Same flow as the advisor speed form.

import SpeedClientSignUpForm from "@/components/speed-client-sign-up-form";

export default function PartnerSpeedClientPage() {
  return <SpeedClientSignUpForm />;
}
